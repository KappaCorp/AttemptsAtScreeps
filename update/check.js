module.exports = {
  updateList: function () {
    var allCreeps = Room.find(FIND_MY_CREEPS);
    for (i = 0; i < allCreeps; i++) {
      switch (allCreeps[i]) {
        case 'HarvBotty': {
          break;
        }
      }
    }
  }	  
  checkDead: function () {
  
  }
}
